# 随行 PRD（MVP）

## 1. 产品概述

**产品名称：** 随行  
**产品定位：** 面向中国用户的国内自由行 AI 旅行规划与执行助手。

随行让用户通过自然语言创建、查看、调整和执行一趟结构化国内旅行。它不是生成旅游攻略文章，而是让用户直接操作一趟旅行。

> Chat is the input, Trip is the product.

当前仅支持中国大陆境内出行。

## 2. 用户问题

国内自由行用户通常需要在地图、攻略、OTA、社交内容和聊天工具之间切换，完成地点筛选、路线安排、预算估算和资源预订。最终结果往往是一份难以修改、难以执行的攻略或收藏夹。

随行解决以下问题：

- 将自然语言需求转成结构化旅行。
- 将每日地点、时间、路线和预算组织为可执行行程。
- 使用真实国内 POI 和地图数据，避免虚构地点或坐标。
- 用户能通过自然语言持续调整当前 Trip。
- 每次修改先展示影响，再由用户确认。
- 提供国内酒店、机票、火车、门票等资源入口，并跳转第三方完成购买。

## 3. 目标用户与场景

**目标用户：** 计划国内城市游、周边游、跨省自由行的中国用户。

典型输入：

- “国庆去上海 3 天，两个人，预算 4000，喜欢咖啡店和拍照，不想太累。”
- “周末去杭州两天，带父母，想看西湖，不想走太多路。”
- “第二天不要去武康路了，换成上海博物馆。”
- “酒店便宜一点，但离地铁不要太远。”

## 4. 产品目标与边界

### MVP 目标

- 用户可通过自然语言创建国内 Trip。
- 系统提取结构化旅行需求，并在创建前允许编辑确认。
- 系统生成每日结构化行程。
- 所有地点匹配真实 POI，并保存 POI ID、地址和坐标。
- 行程与地图联动，支持按天查看路线。
- 用户可通过自然语言修改当前 Trip。
- 修改前生成 Change Preview，确认后再应用。
- 支持酒店、国内机票、火车和门票的推荐与外部跳转。

### MVP 非目标

- 不支持出境游、签证、多币种、境外交通或境外酒店。
- 不完成支付、出票、退改签。
- 不逆向获取 12306 实时余票。
- 不做点评爬虫、旅游社区或复杂实时导航。

## 5. 核心交互逻辑

```text
主界面
  ├─ 创建新旅行
  ├─ 继续规划中的旅行
  ├─ 查看已生成 / 已完成旅行
  └─ 查看收藏的地点与灵感
        ↓
需求确认
        ↓
生成 Trip
        ↓
Trip Workspace
  ├─ 行程
  ├─ 地图
  ├─ 预订
  └─ 问随行（修改当前 Trip）
```

### 5.1 主界面

主界面承担两件事：创建新旅行，以及回到已有旅行。

主要内容：

- 创建入口：“下一站，想去哪？”
- 进行中的旅行：尚未完成、仍可修改的 Trip。
- 我的旅行：已生成、旅行中或已完成的 Trip。
- 收藏：用户保存的目的地、地点、路线或旅行灵感。

优先级：

```text
创建新旅行 → 继续规划中的旅行 → 已生成旅行 → 收藏的地点与灵感
```

用户应能直接输入新需求，或继续最近一趟正在规划的旅行；主界面不是聊天首页。

### 5.2 创建新旅行

用户输入国内旅行需求后，系统提取目的地、日期、人数、预算、兴趣与节奏等字段。仅在目的地、日期或天数等关键字段缺失时追问。

```text
输入自然语言需求
  ↓
AI 提取结构化需求
  ↓
需求确认页逐项编辑
  ↓
生成旅行方案
  ↓
创建 Trip、生成行程并匹配真实 POI
  ↓
进入 Trip Workspace
```

### 5.3 生成后的攻略：Trip Workspace

“攻略”在产品内是一趟可操作的 Trip，而不是一篇文章。

Trip Workspace 包含：

- **行程：** 每日时间轴、地点、停留时间、交通和费用。
- **地图：** 真实地点、编号 Marker、每日路线与距离。
- **预订：** 国内酒店、机票、火车、门票资源入口。
- **问随行：** 当前 Trip 的自然语言控制入口。

顶部展示目的地、日期、人数、预算和 Trip 状态。用户可切换 Day、点击地点查看地图位置，或打开资源推荐。

### 5.4 之前的攻略：历史 Trip

之前生成的攻略不应以聊天记录形式管理，而应以历史 Trip 管理。

Trip 状态：

- `PLANNING`：规划中，可继续修改。
- `READY`：行程已基本确认。
- `TRAVELLING`：旅行中，优先展示今日行程与下一站。
- `COMPLETED`：旅行完成，可回顾、复用或再次创建。

每张 Trip 卡片至少展示目的地、日期、天数、人数、预算和状态。点击后回到对应 Workspace。

### 5.5 收藏

收藏是旅行素材库，不与用户实际 Trip 混在一起。

可收藏内容：

- 城市或目的地。
- 景点、餐厅、咖啡店、酒店等地点。
- 路线或行程片段。
- 旅行灵感。

主要操作：

```text
收藏内容 → 查看详情 → 加入当前 Trip / 基于此创建新 Trip
```

将收藏地点加入当前 Trip 时，系统需重新计算受影响路线、时间和预算，并展示变更预览。

### 5.6 继续修改攻略

用户在某一 Trip 内通过“问随行”提出修改，例如：

- “第二天轻松一点。”
- “加一个安静的咖啡店。”
- “酒店预算降到每晚 700。”
- “第二天不要去武康路，换成上海博物馆。”

```text
用户自然语言指令
  ↓
识别当前 Trip 与受影响 Day
  ↓
解析为结构化操作
  ↓
查询真实 POI / 资源并重算路线、时间、预算
  ↓
展示 Change Preview
  ↓
取消或应用修改
```

应用成功后，行程时间轴、地图 Marker、路线和预算原地同步更新，并显示“行程已更新”。不跳转独立修改页面，也不回到聊天窗口。

## 6. 核心数据模型

### Trip

一趟独立旅行，是系统事实来源。

- `id`
- `user_id`
- `destination`
- `origin`
- `start_date`
- `end_date`
- `traveler_count`
- `total_budget`
- `preferences`
- `pace`
- `status`
- `created_at`
- `updated_at`

### TripDay

- `id`
- `trip_id`
- `day_number`
- `date`
- `title`
- `summary`

### TripPlace

- `id`
- `trip_day_id`
- `order`
- `place_id`
- `place_name`
- `type`
- `start_time`
- `end_time` 或 `duration`
- `description`
- `estimated_cost`
- `transport_to_next`

### Place

真实地点的标准化内部模型：

```json
{
  "id": "internal_place_id",
  "provider": "amap",
  "provider_place_id": "B0XXXX",
  "name": "武康路",
  "address": "上海市徐汇区",
  "latitude": 31.21,
  "longitude": 121.43,
  "category": "attraction"
}
```

### TripChange

- `id`
- `trip_id`
- `status`
- `operations`
- `before_snapshot`
- `after_preview`
- `cost_delta`
- `time_delta`
- `created_at`
- `applied_at`

状态：`DRAFT`、`PENDING_CONFIRMATION`、`APPLIED`、`CANCELLED`、`EXPIRED`。

## 7. 功能需求

### 7.1 首页

- 主输入框：“下一站，想去哪？”
- 支持自然语言输入国内旅行需求。
- 展示进行中、历史和已完成 Trip。
- 展示收藏的地点与灵感。
- 点击任一 Trip 进入对应 Workspace。

### 7.2 需求确认

确认页需展示并允许编辑：

- 目的地、出发地、日期或天数、人数、总预算。
- 兴趣偏好、行程节奏、住宿偏好。
- 必去地点与不希望出现的内容。

用户点击“生成旅行方案”后才创建 Trip；不要求聊天确认。

### 7.3 结构化行程

每个 Day 展示时间、地点、停留建议、地点描述、下一段交通和预估费用。

```text
10:30 武康路
建议停留 90 分钟
  ↓ 步行 12 分钟
12:00 RAC Coffee
午餐 / 咖啡
  ↓ 地铁 28 分钟
14:30 外滩
```

行程不能仅保存为自然语言或 Markdown。单个地点变更时应优先增量更新受影响部分。

### 7.4 动态地图

第一阶段使用高德地图 API。

- 查询国内真实 POI。
- 保存 POI ID、地址、经纬度和分类。
- 按 Day 显示地点、编号 Marker 和路线。
- 显示地点间距离和预计交通时间。
- 行程列表与地图双向联动。
- 行程变更后同步刷新 Marker 和路线。

LLM 不得生成坐标或伪造 POI。前端不得直接依赖高德原始返回格式。

### 7.5 Change Preview

示例：用户说“第二天不要去武康路了，换成上海博物馆。”

系统需识别 `REMOVE 武康路` 与 `ADD 上海博物馆`，查询真实 POI，重算关联路线、时间和预算，然后展示：

- 删除、添加或替换的地点。
- 受影响 Day 与行程段。
- 交通时间、预算、节奏变化。
- 必要时展示路线变化。

未确认前不得修改正式 Trip 数据；应用后需同步更新行程、地图、路线和预算。

### 7.6 国内旅行资源

资源类型：国内酒店、国内机票、火车票、国内景点门票。

```text
推荐资源 → 查看统一资源信息 → 跳转 OTA / 第三方平台购买
```

MVP 不承担支付、出票或退改签。

## 8. 技术架构原则

### AI 层

职责：

- 自然语言转结构化旅行需求。
- 根据需求生成行程建议。
- 将修改请求解析为增量操作。

约束：

- LLM 使用严格 JSON Schema 输出。
- 前端不解析 LLM 原始文本作为业务状态。
- LLM 不直接写入正式 Trip。
- LLM 不生成经纬度、路线距离或 Provider ID。

### Provider 层

```text
业务服务
  ↓
Provider Interface
  ├── PlaceProvider
  ├── RouteProvider
  ├── HotelProvider
  ├── FlightProvider
  ├── TrainProvider
  └── TicketProvider
  ↓
具体 Adapter
  ├── Amap Adapter
  └── 国内旅行资源 Provider Adapter
```

高德负责国内 POI 搜索、地址与坐标、路线、距离和预计交通时间。

### 数据与安全

- Trip、TripDay、TripPlace、TripRoute 等是系统事实来源。
- ConversationMessage 仅是交互记录。
- 关键操作和变更预览必须持久化。
- API Key 与 Secret 仅保存在后端。
- 外部 Provider 失败时不影响已保存的 Trip，并需支持重试或降级。
- 不同 Trip 的数据与 AI 上下文必须隔离。

## 9. MVP 验收标准

- 用户能成功创建一趟国内 Trip。
- 用户能在确认页编辑结构化需求。
- 系统可生成每日结构化行程。
- 行程地点可匹配真实国内 POI。
- 地图可按 Day 展示编号 Marker 与路线。
- 行程与地图在修改后保持同步。
- 用户可通过自然语言发起修改并查看 Change Preview。
- 用户确认后才写入正式 Trip。
- 支持国内酒店、机票、火车和门票的外部跳转入口。

## 10. 成功指标

- Trip 创建完成率。
- 需求确认页完成率。
- 真实 POI 匹配成功率。
- 地图路线生成成功率。
- 行程修改发起率。
- Change Preview 确认率。
- 修改应用成功率。
- 资源外跳点击率。
